#summary Firmware debug

= Firmware debug =

http://hddguardian.googlecode.com/svn/screenshots/050/smartctl-firmware_debug.png

Some units may have firmware that do not report the data correctly when queried (especially some Samsung units). From here you can change the smartctl interpretation of values.

If a device is into the database of smartctl, the eventual firmware debug is already set.